# Express backend URL. The frontend calls it directly from the browser,
# so the public variable is required (baked in at dev/build time).
# Default backend from this assessment: http://localhost:4000
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:4000
# Server-only fallback still supported:
# BACKEND_API_URL=http://localhost:4000
